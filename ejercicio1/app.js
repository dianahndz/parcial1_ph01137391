const { createApp, ref } = Vue

 
        
        return {
            message,
            
            cambiofrase,
        }
        //console.log("hello world in vuel")
    }

  app.mount('#myapp')